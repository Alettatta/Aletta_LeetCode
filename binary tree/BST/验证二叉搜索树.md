# Problem
https://labuladong.online/zh/problem/leetcode/validate-binary-search-tree/description/


# Problem Description
给定一个二叉树的根节点 root，判断其是否是一棵有效的二叉搜索树（BST）。

一棵有效的二叉搜索树定义如下：

节点的左子树只包含严格小于当前节点值的节点。

节点的右子树只包含严格大于当前节点值的节点。

所有左子树和右子树自身也必须是二叉搜索树。


# Solution
注意BST要求严格递增,sorted() 不能区分"相等"的情况

sorted() 只能保证"非递减"(允许有重复元素),但BST的定义通常要求严格递增(不能有重复值)。举例:

pythoninorder = [1, 2, 2, 3]

sorted(inorder) == [1, 2, 2, 3]   # 相等，你的代码会判断为True

但如果一棵"二叉搜索树"中出现了两个值相同的节点,按照LeetCode的标准定义,这不是合法的BST(每个节点的值必须严格大于左子树所有节点、严格小于右子树所有节点)。所以应该额外检查"数组中没有相邻重复元素"。

所以必须要前一个元素<后一个元素

# Code

## LC version

```python
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        # 如果中序遍历数组是有序的，说明是BST
        ans = self.traverse(root)
        for i in range(1, len(ans)):
            if ans[i] <= ans[i-1]:
                return False
        return True
        
    def traverse(self, root):
        list = []
        if root is None:
            return []
        list.extend(self.traverse(root.left))
        list.append(root.val)
        list.extend(self.traverse(root.right))
        return list
```


# Complexity Analysis
- 时间复杂度：O(n)
- 空间复杂度：O(n)


优化空间复杂度：

```python
class Solution:
    def isValidBST(self, root: TreeNode) -> bool:
        return self._isValidBST(root, None, None)

    # 定义：该函数返回 root 为根的子树的所有节点是否满足 max.val > root.val > min.val
    def _isValidBST(self, root: TreeNode, min: TreeNode, max: TreeNode) -> bool:
        # base case
        if root is None:
            return True
        # 若 root.val 不符合 max 和 min 的限制，说明不是合法 BST
        if min is not None and root.val <= min.val:
            return False
        if max is not None and root.val >= max.val:
            return False
        # 根据定义，限定左子树的最大值是 root.val，右子树的最小值是 root.val
        return self._isValidBST(root.left, min, root) and self._isValidBST(root.right, root, max)
```
