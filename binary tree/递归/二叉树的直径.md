# Problem
https://labuladong.online/zh/problem/leetcode/diameter-of-binary-tree/description/


# Problem Description
给定一棵二叉树的根节点 root，返回该二叉树的直径。

二叉树的直径是指树中任意两个节点之间最长路径的长度。这条路径可能经过也可能不经过根节点。

两节点之间路径的长度由它们之间边的数目表示。


# Solution
1. 每一条二叉树的「直径」长度，就是一个节点的左右子树的最大深度之和——遍历每个节点，计算"以该节点为最高点的最长路径"，然后取所有节点中的最大值
2. 对于任意节点 root，如果我们知道：leftMax = 左子树的最大深度，rightMax = 右子树的最大深度，那么以 root 为最高点的最长路径 = leftMax + rightMax
3. 上面的“任意节点”相当于一个父节点，为了计算父节点的路径，我需要知道当前节点的最大深度（其实就是高度）——最大深度 = 1 + max(左子树最大深度, 右子树最大深度)
4. 现在出现一个矛盾：每个节点既要向上汇报自己的深度，又要向下询问孩子的深度；既然每个节点都需要做两件事：汇报深度（给父节点用）、计算直径（给自己用），那递归函数应该返回"深度"，同时在过程中计算"直径"
5. 递归函数：maxDepth(root):
    1. 如果 root 为空，深度为 0
    2. 递归计算左子树深度 leftMax
    3. 递归计算右子树深度 rightMax
    4. 用 leftMax + rightMax 更新全局最大直径
    5. 返回 1 + max(leftMax, rightMax) 给父节点使用
PS. 因为要先用子问题的结果（左右子树深度）才能计算当前节点的直径，所以必须后序遍历（先处理左右子树，再处理当前节点）。

示例：

```python
    1
   / \
  2   3
 / \
4   5
递归执行过程（从叶子开始后序遍历）：
节点4：leftMax=0, rightMax=0 → 直径=0 → 返回深度1
节点5：leftMax=0, rightMax=0 → 直径=0 → 返回深度1
节点2：leftMax=1（来自4）, rightMax=1（来自5）→ 直径=2 → 返回深度2
节点3：leftMax=0, rightMax=0 → 直径=0 → 返回深度1
节点1：leftMax=2（来自2）, rightMax=1（来自3）→ 直径=3 → 返回深度3
最大直径 = max(0, 0, 2, 0, 3) = 3 ✅
路径是 4→2→1→3（3条边）
```

# Code

## LC version

```python
class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        if root is None:
            return 0
            
        # 左右子树的最大高度
        leftMax = self.maxDepth(root.left)
        rightMax = self.maxDepth(root.right)
        # root 的直径
        ans = leftMax + rightMax
        
        # 递归
        return max(ans, max(self.diameterOfBinaryTree(root.left),
                            self.diameterOfBinaryTree(root.right)))

                   
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if root is None:
            return 0
        else:
            leftMax = self.maxDepth(root.left)
            rightMax = self.maxDepth(root.right)
            return 1 + max(leftMax, rightMax)
```
- 时间复杂度：O(n^2)
- 空间复杂度：O(n)
  
更高效的做法：

每个节点都要求自己的最大直径，最后得到最大最大的直径

```python
class Solution:
    def __init__(self):
        # 记录最大直径的长度
        self.maxDiameter = 0

    def diameterOfBinaryTree(self, root: TreeNode) -> int:
        # 每一个节点都要求直径（直径怎么算？左右子树深度相加）
        self.maxDepth(root)
        # 然后求出最大直径
        return self.maxDiameter

    def maxDepth(self, root: TreeNode) -> int:
        if root is None:
            return 0
        leftMax = self.maxDepth(root.left)
        rightMax = self.maxDepth(root.right)
        # 第一步：计算自己的最大直径
        self.maxDiameter = max(self.maxDiameter, leftMax + rightMax)
        # 第二步：告诉父节点我的深度
        return 1 + max(leftMax, rightMax)
```

- 时间复杂度：O(n)
- 空间复杂度：O(n)
