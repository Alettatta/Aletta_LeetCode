# Problem
https://labuladong.online/zh/problem/leetcode/perfect-squares/description/


# Problem Description
给你一个整数 n，返回和为 n 的完全平方数的最少数量。

完全平方数 是一个整数，其值等于另一个整数的平方；换句话说，其值等于一个整数自乘的积。例如，1、4、9 和 16 都是完全平方数，而 3 和 11 不是。



# Solution
和零钱兑换问题完全同理，只不过现在coin换成了完全平方数，amount换成了n

dp[i]: 和为i的时候，所用完全平方数的最小数量

base case: dp[0] = 0

状态转移方程：dp[i] = min(dp[i - j*j] + 1)  # 其中 j*j <= i，设 j^2 = 完全平方数

# Code

## LC version

```python
class Solution:
    def numSquares(self, n: int) -> int:
        dp = [float('inf')] * (n + 1)
        dp[0] = 0

        for i in range(1, n + 1):
            j = 1
            while j * j <= i:
                dp[i] = min(dp[i], dp[i - j*j] + 1)
                j += 1
        
        return dp[n]
```

## ACM version

**ACM 模式的注意点：**

- 需要 import 完整的类（包括 sys、typing 等）
- 数据在标准输入流 stdin 中，全部是原始的文本字符串
- 必须用 print() 手动将结果写到标准输出流 stdout
- 需要写 while 或 for line in sys.stdin 循环处理，直到文件结束（EOF）

```python
import sys

class Solution:
    def numSquares(self, n: int) -> int:
        dp = [float('inf')] * (n + 1)
        dp[0] = 0

        for i in range(1, n + 1):
            j = 1
            while j * j <= i:
                dp[i] = min(dp[i], dp[i - j*j] + 1)
                j += 1
        
        return dp[n]
        

for line in sys.stdin:
    n = int(line.strip())
    result = Solution().numSquares(n)

    print(result)
```


# Complexity Analysis
- 时间复杂度：O(mn)
- 空间复杂度：O(mn)
