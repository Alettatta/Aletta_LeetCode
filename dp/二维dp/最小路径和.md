# Problem
https://labuladong.online/zh/problem/leetcode/minimum-path-sum/description/


# Problem Description

给定一个包含非负整数的 m x n 网格 grid，请找出一条从左上角到右下角的路径，使得路径上的数字总和为最小。

说明： 每次只能向下或者向右移动一步。


# Solution
dp[i][j]：从grid[0][0]到geid[i][j]的最小路径和

base case：第一行+第一列
        
dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])

# Code

## LC version

```python
class Solution:
    def minPathSum(self, grid: List[List[int]]) -> int:
        # dp[i][j]：从grid[0][0]到geid[i][j]的最小路径和
        # base case：第一行+第一列
        # dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])
        
        m, n = len(grid), len(grid[0])
        dp = [[0] * n for _ in range(m)]

        # base case
        # 左上角
        dp[0][0] = grid[0][0]
        # 第一行：只能从左边来
        for j in range(1, n):
            dp[0][j] = dp[0][j - 1] + grid[0][j]
        # 第一列：只能从上面来
        for i in range(1, m):
            dp[i][0] = dp[i - 1][0] + grid[i][0]


        for i in range(1, m):
            for j in range(1, n):
                dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])

        return dp[m - 1][n - 1]
```

## ACM version

**ACM 模式的注意点：**

- 需要 import 完整的类（包括 sys、typing 等）
- 数据在标准输入流 stdin 中，全部是原始的文本字符串
- 必须用 print() 手动将结果写到标准输出流 stdout
- 需要写 while 或 for line in sys.stdin 循环处理，直到文件结束（EOF）

```python
import sys
from typing import List


class Solution:
    def minPathSum(self, grid: List[List[int]]) -> int:
        # dp[i][j]：从grid[0][0]到geid[i][j]的最小路径和
        # base case：第一行+第一列
        # dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])
        
        m, n = len(grid), len(grid[0])
        dp = [[0] * n for _ in range(m)]

        # base case
        # 左上角
        dp[0][0] = grid[0][0]
        # 第一行：只能从左边来
        for j in range(1, n):
            dp[0][j] = dp[0][j - 1] + grid[0][j]
        # 第一列：只能从上面来
        for i in range(1, m):
            dp[i][0] = dp[i - 1][0] + grid[i][0]


        for i in range(1, m):
            for j in range(1, n):
                dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])

        return dp[m - 1][n - 1]


for line in sys.stdin:
    parts = line.split()
    if len(parts) < 2:
        continue
    m, n = int(parts[0]), int(parts[1])
    grid = []
    for _ in range(m):
        row = sys.stdin.readline().split()
        grid.append([int(x) for x in row])
    result = Solution().minPathSum(grid)

    print(result)
```


# Complexity Analysis
- 时间复杂度：O(mn)
- 空间复杂度：O(mn)
