# Problem
https://labuladong.online/zh/problem/leetcode/sliding-window-maximum/description/


# Problem Description
给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到滑动窗口内的 k 个数字。滑动窗口每次向右移动一位。

请返回滑动窗口中的最大值组成的数组（按窗口从左到右的顺序）。


# Key Points
滑动窗口大小固定，所以不是一般窗口，每次移动也就是pop一个元素push一个元素，要返回最大值

因为左边右边都要操作，所以可以使用双端队列，因为要求最大值，所以每次保留的都是最大

使用单调队列，push方法依然在队尾添加元素，但是要把前面比自己小的元素都删掉，直到遇到更大元素才停止删除，这样从队头出来的元素就是最大值





# Code

## LC version

```python
from collections import deque
class MonotonicQueue:
    def __init__(self):
        self.maxq = deque()

    def push(self, n):
        while self.maxq and self.maxq[-1] < n:
            self.maxq.pop()
        self.maxq.append(n)

    def max(self):
        return self.maxq[0]

    def pop(self, n):
        if n == self.maxq[0]:
            self.maxq.popleft() # 删除这个重复的最大值
    
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        window = MonotonicQueue()
        ans = []

        for i in range(len(nums)):
            if i < k - 1: # 窗口前k - 1先加入元素
                window.push(nums[i])
            else:
                window.push(nums[i]) # 窗口向前滑动，加入新数字
                ans.append(window.max()) # 记录当前窗口的最大值
                window.pop(nums[i - k + 1]) # 移出旧数字

        return ans
```

## ACM version

**ACM 模式的注意点：**

- 需要 import 完整的类（包括 sys、typing 等）
- 数据在标准输入流 stdin 中，全部是原始的文本字符串
- 必须用 print() 手动将结果写到标准输出流 stdout
- 需要写 while 或 for line in sys.stdin 循环处理，直到文件结束（EOF）

```python
import sys
from typing import List
from collections import deque


class MonotonicQueue:
    def __init__(self):
        self.maxq = deque()

    def push(self, n):
        while self.maxq and self.maxq[-1] < n:
            self.maxq.pop()
        self.maxq.append(n)

    def max(self):
        return self.maxq[0]

    def pop(self, n):
        if n == self.maxq[0]:
            self.maxq.popleft() # 删除这个重复的最大值
    
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        window = MonotonicQueue()
        ans = []

        for i in range(len(nums)):
            if i < k - 1: # 窗口前k - 1先加入元素
                window.push(nums[i])
            else:
                window.push(nums[i]) # 窗口向前滑动，加入新数字
                ans.append(window.max()) # 记录当前窗口的最大值
                window.pop(nums[i - k + 1]) # 移出旧数字

        return ans

header = None
for line in sys.stdin:
    parts = line.strip().split()
    if not parts:
        continue
    if header is None:
        header = (int(parts[0]), int(parts[1]))
    else:
        n, k = header
        nums = list(map(int, parts))
        result = Solution().maxSlidingWindow(nums, k)

        print(" ".join(map(str, result)))

        header = None
```


# Complexity Analysis
- 时间复杂度：O(n)
- 空间复杂度：O(n)
