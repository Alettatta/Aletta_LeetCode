# Problem
https://leetcode.cn/problems/range-sum-query-2d-immutable/description/


# Problem Description
给定一个二维矩阵 matrix，以下类型的多个请求：

计算其子矩形范围内元素的总和，该子矩阵的 左上角 为 (row1, col1) ，右下角 为 (row2, col2) 。

实现 NumMatrix 类：

NumMatrix(int[][] matrix) 给定整数矩阵 matrix 进行初始化

int sumRegion(int row1, int col1, int row2, int col2) 返回 左上角 (row1, col1) 、右下角 (row2, col2) 所描述的子矩阵的元素 总和 。



# Solution
暴力枚举，时间复杂度O(n^2)

注意任意子矩阵的元素和可以转化成它周边几个大矩阵的元素和的运算：

![子矩阵元素和](../photos/子矩阵元素和.jpeg)

而这四个大矩阵有一个共同的特点，就是左上角都是 (0, 0) 原点。那么做这道题更好的思路和一维数组中的前缀和是非常类似的，我们可以维护一个二维 preSum 数组，专门记录以原点为顶点的矩阵的元素之和

用程序表述就是：matrix[i][j] = preSum[i+1][j+1] - preSum[i][j+1] - preSum[i+1][j] + preSum[i][j]


# Code

## LC version

```python
class NumMatrix:

    def __init__(self, matrix: List[List[int]]):
        m = len(matrix)
        n = len(matrix[0])

        # 特殊情况
        if m == 0 or n == 0:
            return
        
        self.preSum = [[0] * (n + 1) for _ in range(m + 1)]
        for i in range(m + 1):
            for j in range(1, n+ 1):
                self.preSum[i][j] = self.preSum[i - 1][j] + self.preSum[i][j - 1] + matrix[i - 1][j - 1] - self.preSum[i - 1][j - 1]

    def sumRegion(self, x1: int, y1: int, x2: int, y2: int) -> int:
        return (self.preSum[x2 + 1][y2 + 1] - self.preSum[x1][y2 + 1] - self.preSum[x2 + 1][y1] + self.preSum[x1][y1])
```


# Complexity Analysis
- 时间复杂度：O(1)
- 空间复杂度：O(mn)
