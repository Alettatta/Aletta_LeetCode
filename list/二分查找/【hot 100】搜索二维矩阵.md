# Problem
https://labuladong.online/zh/problem/leetcode/search-a-2d-matrix/description/


# Problem Description
给你一个满足下述两条属性的 m x n 整数矩阵：每行中的整数从左到右按非严格递增顺序排列。每行的第一个整数大于前一行的最后一个整数。给你一个整数 target ，如果 target 在矩阵中，返回 true ；否则，返回 false 。


# Key Points
可以把二维数组拉成一维的，用标准二分模板即可

# Code

## ACM version

```python
import sys 

class Solution:
    def searchMatrix(self, matrix, target):
        nums = []
        m, n = len(matrix), len(matrix[0])
        for i in range(m):
            for j in range(n):
                nums.append(matrix[i][j])
        left, right = 0, len(nums) - 1
        while left <= right:
            mid = (left + right) // 2
            if nums[mid] == target: 
                return 'true'
            elif nums[mid] < target:
                left = mid + 1
            elif nums[mid] > target:
                right = mid - 1
        return 'false'
        
data = sys.stdin.read().strip().split("\n")
idx = 0
while idx < len(data):
    m, n, target = map(int, data[idx].split()); idx += 1
    matrix = []
    for _ in range(m): 
        matrix.append(list(map(int, data[idx].split())))
        idx += 1
    ans = Solution().searchMatrix(matrix, target)
    print(ans)
```


# Complexity Analysis
- 时间复杂度：O(logn)
- 空间复杂度：O(1)
