# Problem
https://labuladong.online/zh/problem/core/difference-array/description/


# Problem Description
给你一个长度为 n 的整数数组 nums，以及一个操作列表 operations。

每个操作由三个整数 [i, j, val] 组成，表示对数组 nums 的闭区间 [i, j] 中的所有元素都加上 val。

请你实现一个函数，对数组 nums 执行所有操作后，返回最终的结果数组。


# Code

## ACM version

**ACM 模式的注意点：**

- 需要 import 完整的类（包括 sys、typing 等）
- 数据在标准输入流 stdin 中，全部是原始的文本字符串
- 必须用 print() 手动将结果写到标准输出流 stdout
- 需要写 while 或 for line in sys.stdin 循环处理，直到文件结束（EOF）

法一：

```python
# 已知长度为n的nums，operations=[[i,j,val]……]，对nums[i…j]全部加上val

import sys

class Solution:
    def getModifiedArray(self, nums, operations):
        n = len(nums)
        
        # 构造差分数组
        diff = [0] * n 
        diff[0] = nums[0]
        for i in range(1, n): # 注意，整个数组长度为n，但是i只要遍历到n-1就ok，所以不是range(1,n+1)
            diff[i] = nums[i] - nums[i - 1]

        # 首先对差分数组进行区间[i,j]的+val操作
        for op in operations:
            i, j ,val = op[0], op[1], op[2]
            diff[i] += val
            if j + 1 < n:
                diff[j + 1] -= val
                
        # 构造结果数组
        ans = [0] * n
        ans[0] = diff[0]
        for i in range(1, n):
            ans[i] = ans[i - 1] + diff[i]

        return ans


data = sys.stdin.read().strip().split()
idx = 0
while idx < len(data):
    n = int(data[idx]); idx += 1 # 提取数组长度
    nums = [int(x) for x in data[idx: idx + n]]; idx += n # 提取数组
    m = int(data[idx]); idx += 1 # 提取oprations组数
    operations = []
    for _ in range(m):
        op = [int(x) for x in data[idx: idx + 3]]; idx += 3
        operations.append(op)
    result = Solution().getModifiedArray(nums, operations)
    for num in result:
        print(num)
```

法二：

```python
import sys
from typing import List


class Solution:
    def getModifiedArray(self, nums: List[int], operations: List[List[int]]) -> List[int]:
        n = len(nums)
        
        # 构造差分数组
        diff = [0] * n 
        diff[0] = nums[0]
        for i in range(1, n): # 注意，整个数组长度为n，但是i只要遍历到n-1就ok，所以不是range(1,n+1)
            diff[i] = nums[i] - nums[i - 1]

        # 首先对差分数组进行区间[i,j]的+val操作
        for op in operations:
            i, j ,val = op[0], op[1], op[2]
            diff[i] += val
            if j + 1 < n:
                diff[j + 1] -= val
                
        # 构造结果数组
        ans = [0] * n
        ans[0] = diff[0]
        for i in range(1, n):
            ans[i] = ans[i - 1] + diff[i]

        return ans


lines = sys.stdin.read().strip().split('\n') # 按行拆分
idx = 0
while idx < len(lines):
    n = int(lines[idx]); idx += 1 # 第一行，提取数组长度n
    nums = list(map(int, lines[idx].split())); idx += 1 # 第二行，nums数组
    m = int(lines[idx]); idx += 1 # 第三行，operations个数
    operations = []
    for _ in range(m):
        op = list(map(int, lines[idx].split()))
        operations.append(op)
        idx += 1
    result = Solution().getModifiedArray(nums, operations)
    for num in result:
        print(num)
```


# Complexity Analysis
- 时间复杂度：O(n)
- 空间复杂度：O(n)
